---
phase: 04-workout-logger
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/app/(main)/workout/page.tsx
  - src/components/workout/WorkoutLogger.tsx
  - src/components/workout/ExercisePickerSheet.tsx
  - src/components/workout/SetRow.tsx
  - src/components/workout/RestTimerWidget.tsx
  - src/components/workout/PRBadge.tsx
  - tests/components/SetRow.test.ts
autonomous: true
requirements:
  - WORK-01
  - WORK-02
  - WORK-04

must_haves:
  truths:
    - "User can start a freestyle workout, add exercises via picker, enter sets with reps/weight, and finish"
    - "Exercise picker bottom sheet has search and muscle-group tabs"
    - "Set row pre-fills from previous set values and confirms with one tap"
    - "Rest timer floats above content and counts down with sound/vibration alert"
    - "PR badge appears inline when a new personal record is set"
  artifacts:
    - path: "src/components/workout/WorkoutLogger.tsx"
      provides: "Single-page workout logging UI"
      contains: "WorkoutLogger"
    - path: "src/components/workout/ExercisePickerSheet.tsx"
      provides: "Bottom sheet exercise picker with search and tabs"
      contains: "Drawer"
    - path: "src/components/workout/SetRow.tsx"
      provides: "Inline set row with NumberField inputs"
      contains: "NumberField"
    - path: "src/components/workout/RestTimerWidget.tsx"
      provides: "Floating timer bubble"
      contains: "setInterval"
    - path: "src/components/workout/PRBadge.tsx"
      provides: "Neon glow PR badge"
      contains: "Trophy"
  key_links:
    - from: "src/components/workout/WorkoutLogger.tsx"
      to: "src/stores/useWorkoutStore.ts"
      via: "useWorkoutStore hook calls"
      pattern: "useWorkoutStore"
    - from: "src/components/workout/ExercisePickerSheet.tsx"
      to: "src/lib/exercise-filter.ts"
      via: "filterExercises for search"
      pattern: "filterExercises"
    - from: "src/components/workout/SetRow.tsx"
      to: "src/lib/pr-detection.ts"
      via: "isNewPR check on confirm"
      pattern: "isNewPR"
---

<objective>
Build the complete workout logging UI: single-page logger with inline exercises and sets, exercise picker bottom sheet, rest timer floating widget, and PR badge. This is the core user-facing feature of Phase 4.

Purpose: Implements the primary logging flow (D-01 through D-07, D-12, D-13) — the heart of the workout tracker.
Output: Fully functional workout logging page at /workout.
</objective>

<execution_context>
@/home/appuser/workspace/rip-zone/.claude/get-shit-done/workflows/execute-plan.md
@/home/appuser/workspace/rip-zone/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-workout-logger/04-CONTEXT.md
@.planning/phases/04-workout-logger/04-RESEARCH.md
@.planning/phases/04-workout-logger/04-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->
From src/stores/useWorkoutStore.ts (extended):
```typescript
// Actions: startSession, finishSession, addExercise, confirmSet, updatePendingSet,
// startTimer, pauseTimer, resumeTimer, adjustTimer, dismissTimer, tickTimer, tickElapsed
```

From src/lib/pr-detection.ts:
```typescript
export type PRMap = Map<number, number>
export function computePRs(sets: Pick<SetLogRecord, 'reps' | 'weightKg'>[]): PRMap
export function isNewPR(newSet: { reps: number; weightKg: number }, historicPRs: PRMap): boolean
```

From src/lib/exercise-filter.ts (existing):
```typescript
export function filterExercises(exercises: Exercise[], query: string, filters?: { muscle?: MuscleSlug; equipment?: EquipmentType }): Exercise[]
```

From @base-ui/react (installed):
- Drawer: Root, Portal, Backdrop, Popup, Title, Description, Close
- NumberField: Root, Group, Input, Increment, Decrement
- Tabs: Root, List, Tab, Panel
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WorkoutLogger page, ExercisePickerSheet, and SetRow with PRBadge</name>
  <files>src/app/(main)/workout/page.tsx, src/components/workout/WorkoutLogger.tsx, src/components/workout/ExercisePickerSheet.tsx, src/components/workout/SetRow.tsx, src/components/workout/PRBadge.tsx, tests/components/SetRow.test.ts</files>
  <read_first>src/stores/useWorkoutStore.ts, src/lib/pr-detection.ts, src/lib/exercise-filter.ts, src/components/exercise-library/WarmupSheet.tsx, src/types/workout.ts, src/lib/db/workouts.ts, data/exercises.json</read_first>
  <action>
1. **PRBadge component** (`src/components/workout/PRBadge.tsx`):
   - `'use client'`; import `Trophy` from lucide-react
   - Simple presentational component: `export function PRBadge() { return <span className="inline-flex items-center gap-1 px-2 py-0.5 rounded-full bg-primary/10 text-primary text-[12px] font-semibold shadow-[0_0_8px_2px] shadow-primary/50 animate-pulse"><Trophy size={12} />PR</span> }`
   - Per D-12: neon glow via shadow-primary/50

2. **SetRow component** (`src/components/workout/SetRow.tsx`):
   - `'use client'`; import `NumberField` from `@base-ui/react/number-field`, `Check` and `Minus` and `Plus` from lucide-react, `PRBadge`
   - Props: `{ setNumber: number; defaultReps: number; defaultWeightKg: number; isPR: boolean; confirmed: boolean; onConfirm: (reps: number, weightKg: number) => void }`
   - Internal state: `reps` (init from defaultReps), `weightKg` (init from defaultWeightKg)
   - Layout: horizontal row with set number label, NumberField for reps (min=1, max=100, step=1), NumberField for weightKg (min=0, step=0.5, display "kg" suffix), confirm button (Check icon), PRBadge if isPR
   - Per D-03: pre-fills from defaultReps/defaultWeightKg. If confirmed=true, row is read-only with muted styling.
   - Per D-04: kg only, show "kg" label next to weight input
   - NumberField uses `onValueChange` (not `onChange`) per Research pitfall 5

3. **ExercisePickerSheet** (`src/components/workout/ExercisePickerSheet.tsx`):
   - `'use client'`; import `Drawer` from `@base-ui/react/drawer`, `Tabs` from `@base-ui/react/tabs`, `Search` from lucide-react
   - Props: `{ open: boolean; onOpenChange: (open: boolean) => void; exercises: Exercise[]; onSelectExercise: (exercise: Exercise) => void }`
   - Per D-02: bottom sheet with search bar at top + muscle-group tabs
   - Search: controlled input that filters exercises using `filterExercises` from `src/lib/exercise-filter.ts`
   - Tabs: "Recent" (placeholder — shows first 10 exercises), "All" (full list), plus one tab per major muscle group: Chest, Back, Shoulders, Arms, Legs, Core. Filter by `primaryMuscles` containing the group slug.
   - Each exercise item shows name + primary muscles as small text. Tap calls onSelectExercise and closes sheet.
   - Uses `@base-ui/react` Drawer (NOT Dialog) for swipe-dismiss per Research pitfall 3:
     ```
     <Drawer.Root open={open} onOpenChange={onOpenChange}>
       <Drawer.Portal><Drawer.Backdrop .../><Drawer.Popup ...>
         <Drawer.Title className="sr-only">Pick an exercise</Drawer.Title>
         {/* search + tabs + list */}
       </Drawer.Popup></Drawer.Portal>
     </Drawer.Root>
     ```

4. **WorkoutLogger component** (`src/components/workout/WorkoutLogger.tsx`):
   - `'use client'`; imports useWorkoutStore, ExercisePickerSheet, SetRow, PRBadge
   - Per D-01: single scrollable page layout
   - Top section: workout elapsed timer (mm:ss format from `elapsedSeconds`), session status. Uses `useEffect` with `setInterval` calling `tickElapsed` every 1000ms when session active.
   - Exercise list: maps `activeExercises` from store. Each exercise block shows exercise name (look up from exercises data), its confirmed sets as SetRow components (confirmed=true), and a pending SetRow (confirmed=false) for the next set. "+Set" button below last set.
   - For each exercise, compute PRs: query all historical sets for that exercise slug from Dexie (via a useEffect that loads on mount), call `computePRs`, then for each confirmed set check `isNewPR`.
   - "+Exercise" button at bottom opens ExercisePickerSheet. On select: calls `addExercise(slug, restSeconds)` from store, auto-starts rest timer via `startTimer(restSeconds, slug)` if the exercise has restSeconds.
   - "Finish Workout" button at very bottom (per D-01). Calls `finishSession()`.
   - If no active session, show "Start Workout" button that calls `startSession()`.
   - Exercises data: pass exercise data from the page component (loaded via SSG or imported from seed data).

5. **Workout page** (`src/app/(main)/workout/page.tsx`):
   - `'use client'` page. Replace the stub from Plan 01.
   - Imports WorkoutLogger and passes exercises data. For now, import exercises from seed data using a client-side approach: use a `useState` + `useEffect` that fetches from `/api/exercises` or imports from a cached source. The simplest approach: load exercises via a module-level import of the JSON (Next.js supports JSON imports).
   - Wrap in `<main className="flex-1 pb-20">` (pb-20 for bottom nav clearance).

6. **SetRow test** (`tests/components/SetRow.test.ts`):
   - Test: renders with defaultReps=10 and defaultWeightKg=60, verify inputs show those values
   - Test: onConfirm called with correct reps/weightKg when check button clicked
   - Test: PRBadge visible when isPR=true, hidden when isPR=false
  </action>
  <verify>
    <automated>cd /home/appuser/workspace/rip-zone && npx tsc --noEmit 2>&1 | head -20 && npx vitest run tests/components/SetRow.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/workout/WorkoutLogger.tsx contains `useWorkoutStore`
    - src/components/workout/WorkoutLogger.tsx contains `startSession` and `finishSession`
    - src/components/workout/ExercisePickerSheet.tsx contains `Drawer` import from `@base-ui/react/drawer`
    - src/components/workout/ExercisePickerSheet.tsx contains `filterExercises`
    - src/components/workout/SetRow.tsx contains `NumberField` import from `@base-ui/react/number-field`
    - src/components/workout/SetRow.tsx contains `onValueChange` (not onChange for NumberField)
    - src/components/workout/PRBadge.tsx contains `shadow-primary/50`
    - src/app/(main)/workout/page.tsx contains `WorkoutLogger`
    - TypeScript compiles with no errors
  </acceptance_criteria>
  <done>Workout logger page is functional: user can start session, add exercises via picker, enter sets with pre-filled values, see PR badges, and finish workout</done>
</task>

<task type="auto">
  <name>Task 2: Rest timer floating widget with sound and vibration</name>
  <files>src/components/workout/RestTimerWidget.tsx</files>
  <read_first>src/stores/useWorkoutStore.ts, src/components/workout/WorkoutLogger.tsx</read_first>
  <action>
1. **RestTimerWidget** (`src/components/workout/RestTimerWidget.tsx`):
   - `'use client'`; import `Timer`, `Play`, `Pause`, `Plus`, `Minus`, `X` from lucide-react
   - Reads timer state from `useWorkoutStore`: `timer.running`, `timer.remaining`, `timer.total`
   - Per D-05: floating mini-widget, fixed position bottom-20 right-4 z-40 (above content, below drawers)
   - Two states: collapsed bubble and expanded controls
   - **Collapsed**: round button showing timer icon + remaining time in mm:ss mono font. Tap to expand. Classes: `fixed bottom-20 right-4 z-40 flex items-center gap-2 bg-card border border-primary/50 rounded-full px-3 py-2 shadow-lg shadow-primary/20`
   - **Expanded**: larger card with: remaining time (large), progress bar (remaining/total), Pause/Resume button, +15s button, -15s button, Dismiss (X) button. Classes: `fixed bottom-20 right-4 z-40 bg-card border border-border rounded-2xl p-4 shadow-xl w-56`
   - **Timer tick**: `useEffect` with `setInterval(1000ms)` that calls `tickTimer()` when `timer.running && timer.remaining > 0`
   - **Timer completion** (when remaining reaches 0):
     - Sound: create `AudioContext`, create `OscillatorNode` at 880Hz (A5), connect to destination, start, stop after 0.2s. Wrap in try/catch for environments where AudioContext is unavailable.
     - Vibration: `navigator.vibrate?.([200, 100, 200])` — double pulse pattern. Optional chaining for iOS Safari compatibility per Research.
     - Auto-dismiss timer via `dismissTimer()`
   - **Per D-06**: timer auto-fills handled by WorkoutLogger (passes restSeconds to startTimer on set confirm). Widget just displays/controls.
   - **Per D-07**: sound + vibration on zero per above.
   - Timer visibility: only render when `timer.remaining > 0 || timer.running` (hidden when no timer active).
   - Format helper: `const formatTime = (s: number) => \`\${Math.floor(s/60)}:\${String(s%60).padStart(2,'0')}\``

2. **Integrate into WorkoutLogger**: Import RestTimerWidget and render it at the bottom of WorkoutLogger component (outside the scrollable area, as a sibling). It positions itself via `fixed`.
  </action>
  <verify>
    <automated>cd /home/appuser/workspace/rip-zone && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/workout/RestTimerWidget.tsx contains `AudioContext`
    - src/components/workout/RestTimerWidget.tsx contains `navigator.vibrate?.(`
    - src/components/workout/RestTimerWidget.tsx contains `setInterval`
    - src/components/workout/RestTimerWidget.tsx contains `useWorkoutStore`
    - src/components/workout/RestTimerWidget.tsx contains `fixed bottom-20 right-4 z-40`
    - src/components/workout/WorkoutLogger.tsx contains `RestTimerWidget`
    - TypeScript compiles with no errors
  </acceptance_criteria>
  <done>Floating rest timer widget counts down between sets, plays beep + vibrates on completion, and can be paused/adjusted/dismissed</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npx vitest run tests/components/SetRow.test.ts` passes
- Workout page renders at /workout with full logging flow
- Timer widget appears when rest timer is started
</verification>

<success_criteria>
- Complete workout logging flow: start session, pick exercise, enter sets, see PRs, finish
- Exercise picker is a swipe-dismiss bottom sheet with search and muscle-group tabs per D-02
- Set rows use NumberField with pre-fill per D-03, kg-only per D-04
- Rest timer floats per D-05, auto-fills per D-06, alerts per D-07
- PR badge shows neon glow per D-12, tracks per-rep-count per D-13
</success_criteria>

<output>
After completion, create `.planning/phases/04-workout-logger/04-03-SUMMARY.md`
</output>
