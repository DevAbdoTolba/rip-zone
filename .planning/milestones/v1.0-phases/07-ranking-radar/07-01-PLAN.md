---
phase: 07-ranking-radar
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/ranking.ts
  - tests/lib/ranking.test.ts
  - src/lib/db/workouts.ts
  - src/hooks/useRankingData.ts
autonomous: true
requirements: [RANK-01, RANK-02, RANK-03]

must_haves:
  truths:
    - "computeTierRank returns correct TierRank for any total volume value"
    - "computeSubTierProgress returns 0-1 fraction within current tier"
    - "computeRadarAxes returns 5-axis scores (push/pull/legs/core/conditioning) normalized 0-100"
    - "Dexie v3 schema adds lastSeenTier table without breaking existing data"
  artifacts:
    - path: "src/lib/ranking.ts"
      provides: "Pure ranking computation functions"
      exports: ["computeTierRank", "computeSubTierProgress", "computeRadarAxes", "TIER_THRESHOLDS", "CATEGORY_MAP"]
    - path: "tests/lib/ranking.test.ts"
      provides: "Unit tests for all ranking computations"
      min_lines: 60
    - path: "src/lib/db/workouts.ts"
      provides: "Dexie v3 with lastSeenTier table"
      contains: "lastSeenTier"
    - path: "src/hooks/useRankingData.ts"
      provides: "Client hook aggregating Dexie data into ranking results"
      exports: ["useRankingData"]
  key_links:
    - from: "src/lib/ranking.ts"
      to: "src/types/ranking.ts"
      via: "TierRank enum import"
      pattern: "import.*TierRank.*from.*@/types"
    - from: "src/hooks/useRankingData.ts"
      to: "src/lib/db/workouts.ts"
      via: "Dexie query for sets and exercises"
      pattern: "workoutsDb"
    - from: "src/hooks/useRankingData.ts"
      to: "src/lib/ranking.ts"
      via: "calls compute functions"
      pattern: "computeTierRank|computeRadarAxes|computeSubTierProgress"
---

<objective>
Build the ranking computation engine and data hook. All pure functions are TDD'd: tier computation from total volume, sub-tier progress fraction, and 5-axis radar scores from per-category volume. Dexie schema upgraded to v3 for lastSeenTier tracking.

Purpose: Establishes all computation logic before any UI is built, ensuring correctness via tests.
Output: Tested ranking functions, Dexie v3 schema, useRankingData hook.
</objective>

<execution_context>
@/home/appuser/workspace/rip-zone/.claude/get-shit-done/workflows/execute-plan.md
@/home/appuser/workspace/rip-zone/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ranking-radar/07-CONTEXT.md

<interfaces>
From src/types/ranking.ts:
```typescript
export enum TierRank {
  Iron = 'Iron',
  Bronze = 'Bronze',
  Silver = 'Silver',
  Gold = 'Gold',
  Platinum = 'Platinum',
  Diamond = 'Diamond',
  Elite = 'Elite',
}

export interface RankState {
  id: 'singleton'
  tier: TierRank
  subTierProgress: number
  lastUpdatedAt: number
}
```

From src/lib/db/workouts.ts:
```typescript
export interface SetLogRecord {
  id: SetLogId
  exerciseInSessionId: ExerciseLogId
  setNumber: number
  reps: number
  weightKg: number
  completedAt: number
}

export interface ExerciseInSessionRecord {
  id: ExerciseLogId
  sessionId: WorkoutSessionId
  exerciseSlug: ExerciseSlug
  orderIndex: number
}

export class WorkoutsDatabase extends Dexie {
  sessions!: EntityTable<WorkoutSessionRecord, 'id'>
  exercisesInSession!: EntityTable<ExerciseInSessionRecord, 'id'>
  sets!: EntityTable<SetLogRecord, 'id'>
  planProgress!: EntityTable<PlanProgressRecord, 'id'>
  lastUsedRest!: EntityTable<LastUsedRestRecord, 'exerciseSlug'>
  // Currently at version(2)
}
```

From data/exercises.json (shape):
```json
{
  "slug": "flat-bench-press",
  "primaryMuscles": ["chest"],
  "secondaryMuscles": ["triceps", "front-deltoid"]
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD ranking computation functions</name>
  <files>src/lib/ranking.ts, tests/lib/ranking.test.ts</files>
  <read_first>src/types/ranking.ts, src/types/workout.ts, data/exercises.json</read_first>
  <behavior>
    - computeTierRank(0) returns TierRank.Iron
    - computeTierRank(4999) returns TierRank.Iron
    - computeTierRank(5000) returns TierRank.Bronze
    - computeTierRank(25000) returns TierRank.Silver
    - computeTierRank(100000) returns TierRank.Gold
    - computeTierRank(300000) returns TierRank.Platinum
    - computeTierRank(750000) returns TierRank.Diamond
    - computeTierRank(1500000) returns TierRank.Elite
    - computeTierRank(2000000) returns TierRank.Elite (caps at Elite)
    - computeSubTierProgress(0) returns 0 (Iron floor)
    - computeSubTierProgress(2500) returns 0.5 (halfway Iron->Bronze)
    - computeSubTierProgress(5000) returns 0 (just entered Bronze)
    - computeSubTierProgress(15000) returns 0.5 (halfway Bronze->Silver)
    - computeSubTierProgress(1500000) returns 1 (Elite has no next tier, full)
    - computeRadarAxes with empty volumes returns { push: 0, pull: 0, legs: 0, core: 0, conditioning: 0 }
    - computeRadarAxes normalizes each axis 0-100 relative to tier max volume
    - CATEGORY_MAP maps muscle slugs: chest/shoulders/triceps->Push, back/biceps->Pull, quads/hamstrings/glutes/calves->Legs, abs/obliques->Core, cardio->Conditioning
  </behavior>
  <action>
    RED: Create tests/lib/ranking.test.ts with all behavior cases above. Import from '@/lib/ranking'. Run tests - all must fail (module not found).

    GREEN: Create src/lib/ranking.ts with:

    1. TIER_THRESHOLDS constant: ordered array of { tier: TierRank, minVolume: number }
       - Iron: 0, Bronze: 5000, Silver: 25000, Gold: 100000, Platinum: 300000, Diamond: 750000, Elite: 1500000

    2. CATEGORY_MAP: Record<string, 'push' | 'pull' | 'legs' | 'core' | 'conditioning'>
       Map muscle group slugs to 5 categories per CONTEXT.md decision:
       - chest, shoulders, triceps, front-deltoid, lateral-deltoid, rear-deltoid -> push
       - back, biceps, lats, rhomboids, trapezius, rear-deltoid -> pull (rear-deltoid maps to both; use pull as primary)
       - quads, hamstrings, glutes, calves, hip-flexors, adductors -> legs
       - abs, obliques, rectus-abdominis, external-obliques, erector-spinae, lower-back -> core
       - All unmatched slugs default to conditioning

    3. computeTierRank(totalVolume: number): TierRank
       - Walk TIER_THRESHOLDS in reverse, return first where totalVolume >= minVolume

    4. computeSubTierProgress(totalVolume: number): number
       - Find current tier index in TIER_THRESHOLDS
       - If Elite (last tier), return 1
       - Otherwise: (totalVolume - currentMin) / (nextMin - currentMin)

    5. computeRadarAxes(categoryVolumes: Record<string, number>, tierMaxVolume: number): { push, pull, legs, core, conditioning } (each 0-100)
       - For each axis: Math.min(100, Math.round((categoryVolumes[axis] ?? 0) / Math.max(1, tierMaxVolume) * 100))

    Run tests - all must pass.

    REFACTOR: Extract RadarAxes type. Ensure all exports are named exports.
  </action>
  <verify>
    <automated>cd /home/appuser/workspace/rip-zone && npx vitest run tests/lib/ranking.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "computeTierRank" src/lib/ranking.ts
    - grep -q "computeSubTierProgress" src/lib/ranking.ts
    - grep -q "computeRadarAxes" src/lib/ranking.ts
    - grep -q "TIER_THRESHOLDS" src/lib/ranking.ts
    - grep -q "CATEGORY_MAP" src/lib/ranking.ts
    - grep -q "TierRank.Iron" tests/lib/ranking.test.ts
    - grep -q "TierRank.Elite" tests/lib/ranking.test.ts
    - All tests pass
  </acceptance_criteria>
  <done>All 15+ unit tests pass. computeTierRank, computeSubTierProgress, computeRadarAxes are pure, tested, exported.</done>
</task>

<task type="auto">
  <name>Task 2: Dexie v3 schema + useRankingData hook</name>
  <files>src/lib/db/workouts.ts, src/hooks/useRankingData.ts</files>
  <read_first>src/lib/db/workouts.ts, src/lib/ranking.ts, src/types/ranking.ts, data/exercises.json, src/hooks/useStrainMap.ts</read_first>
  <action>
    1. Update src/lib/db/workouts.ts:
       - Add LastSeenTierRecord interface: { id: 'singleton', tier: TierRank }
       - Add lastSeenTier EntityTable<LastSeenTierRecord, 'id'> to WorkoutsDatabase class
       - Add version(3).stores() that includes all v2 tables PLUS lastSeenTier: 'id'
       - Do NOT modify version(1) or version(2) blocks

    2. Create src/hooks/useRankingData.ts:
       - 'use client' directive
       - Hook returns { tier, subTierProgress, radarAxes, totalVolume, weekSummary, lastSeenTier, setLastSeenTier, isLoading }
       - Use dynamic import pattern for Dexie (same as useStrainMap): const { workoutsDb } = await import('@/lib/db/workouts')
       - Query all sets via workoutsDb.sets.toArray()
       - Query all exercisesInSession to map exerciseInSessionId -> exerciseSlug
       - Load exercises.json (import from '@/data/exercises.json' or fetch) to get primaryMuscles per slug
       - Compute totalVolume = sum of (reps * weightKg) across all sets
       - Compute per-category volumes by mapping exercise slug -> primaryMuscles -> CATEGORY_MAP -> sum volumes
       - Call computeTierRank(totalVolume), computeSubTierProgress(totalVolume), computeRadarAxes(categoryVolumes, tierMaxVolume)
       - tierMaxVolume = next tier threshold (or current if Elite)
       - weekSummary: filter sessions from last 7 days, count sessions, sum their volume
       - lastSeenTier: read from workoutsDb.lastSeenTier.get('singleton')
       - setLastSeenTier(tier): write to workoutsDb.lastSeenTier.put({ id: 'singleton', tier })
       - Use useState + useEffect with cancelled flag pattern (same as useStrainMap hook)
       - Depend on workoutsDb liveQuery or simple re-fetch approach with activeSession dependency
  </action>
  <verify>
    <automated>cd /home/appuser/workspace/rip-zone && npx vitest run tests/lib/ranking.test.ts && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "version(3)" src/lib/db/workouts.ts
    - grep -q "lastSeenTier" src/lib/db/workouts.ts
    - grep -q "useRankingData" src/hooks/useRankingData.ts
    - grep -q "computeTierRank" src/hooks/useRankingData.ts
    - grep -q "computeRadarAxes" src/hooks/useRankingData.ts
    - grep -q "'use client'" src/hooks/useRankingData.ts
    - TypeScript compiles with no errors
  </acceptance_criteria>
  <done>Dexie v3 has lastSeenTier table. useRankingData hook aggregates workout history into tier, progress, radar axes, and week summary. TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
- All ranking unit tests pass
- TypeScript compiles with no errors
- Dexie version 3 schema is additive (v1 and v2 untouched)
</verification>

<success_criteria>
Pure ranking computation functions are tested and correct. useRankingData hook provides all data the UI needs. Dexie lastSeenTier table exists for celebration tracking.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ranking-radar/07-01-SUMMARY.md`
</output>
