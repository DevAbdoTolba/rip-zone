---
phase: 07-ranking-radar
plan: 03
type: execute
wave: 2
depends_on: ["07-02"]
files_modified:
  - e2e/ranking.spec.ts
autonomous: false
requirements: [RANK-01, RANK-02, RANK-03, RANK-04]

must_haves:
  truths:
    - "E2E tests confirm ranking page loads and shows empty state"
    - "E2E tests confirm radar chart SVG renders with 5 axis labels"
    - "E2E tests confirm progress bar element exists"
    - "E2E tests confirm BottomNav has Ranking tab"
    - "Human verifies visual quality of radar chart, tier badge, progress bar, and celebration"
  artifacts:
    - path: "e2e/ranking.spec.ts"
      provides: "Playwright E2E tests for RANK-01 through RANK-04"
      min_lines: 40
  key_links:
    - from: "e2e/ranking.spec.ts"
      to: "/ranking"
      via: "page.goto"
      pattern: "page.goto.*ranking"
---

<objective>
Write E2E tests for the ranking page and conduct human verification of all visual elements.

Purpose: Validates all RANK requirements work end-to-end before shipping.
Output: Passing E2E tests, human-verified ranking page.
</objective>

<execution_context>
@/home/appuser/workspace/rip-zone/.claude/get-shit-done/workflows/execute-plan.md
@/home/appuser/workspace/rip-zone/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-ranking-radar/07-CONTEXT.md
@.planning/phases/07-ranking-radar/07-01-SUMMARY.md
@.planning/phases/07-ranking-radar/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: E2E tests for ranking page</name>
  <files>e2e/ranking.spec.ts</files>
  <read_first>e2e/muscle-map.spec.ts, e2e/workout.spec.ts, src/app/(main)/ranking/page.tsx, src/components/ranking/RankingDashboard.tsx, src/components/ranking/RadarChart.tsx, src/components/bottom-nav/BottomNav.tsx</read_first>
  <action>
    Create e2e/ranking.spec.ts with Playwright tests:

    1. RANK-01: Ranking page loads with tier display
       - Navigate to /ranking
       - Expect heading "Ranking" visible
       - Expect text "Log your first workout" visible (empty state since no Dexie data)
       - Expect a link to /workout in the empty state CTA

    2. RANK-02: Radar chart renders with 5 axes
       - Navigate to /ranking
       - Expect SVG element present (the radar chart)
       - Expect text labels "Push", "Pull", "Legs", "Core", "Conditioning" visible within SVG or adjacent

    3. RANK-03: Progress bar renders
       - Navigate to /ranking
       - In empty state, progress bar may not show (or shows at 0)
       - Verify the progress bar container element exists using data-testid="progress-bar" or role="progressbar"

    4. RANK-04: Celebration (structural test)
       - Verify CelebrationOverlay component is NOT visible on empty state (no tier change to celebrate)
       - This is primarily verified by human in checkpoint below

    5. Navigation: BottomNav has Ranking tab
       - Navigate to /
       - Expect link with text "Ranking" in nav
       - Click Ranking link
       - Expect URL to be /ranking

    Use the same E2E patterns as existing tests:
    - import { test, expect } from '@playwright/test'
    - Use page.goto('http://localhost:3001/ranking')
    - Use established DB cleanup pattern from e2e tests: page.evaluate(() => { ... indexedDB.databases() ... })
    - Add test.beforeEach with DB cleanup for isolation
  </action>
  <verify>
    <automated>cd /home/appuser/workspace/rip-zone && npx playwright test e2e/ranking.spec.ts --reporter=list 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "RANK-01" e2e/ranking.spec.ts
    - grep -q "RANK-02" e2e/ranking.spec.ts
    - grep -q "RANK-03" e2e/ranking.spec.ts
    - grep -q "RANK-04" e2e/ranking.spec.ts
    - grep -q "ranking" e2e/ranking.spec.ts
    - grep -q "Push" e2e/ranking.spec.ts
    - grep -q "Ranking" e2e/ranking.spec.ts
    - All Playwright tests pass
  </acceptance_criteria>
  <done>E2E tests pass for all 4 RANK requirements covering empty state, radar chart axes, progress bar, celebration guard, and BottomNav integration.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of ranking page</name>
  <what-built>
    Complete ranking page with:
    - 5-axis radar chart (inline SVG) as hero element
    - Tier badge with colored icon and tier name
    - Sub-tier progress bar with animated fill
    - Weekly summary card (sessions + volume)
    - Empty state with radar outline and "Log your first workout" CTA
    - Celebration overlay with CSS confetti (triggers on tier advance)
    - 5-tab BottomNav with Ranking tab
  </what-built>
  <how-to-verify>
    1. Open http://localhost:3001/ranking
    2. Verify empty state: radar chart outline visible with all axes at 0, "Log your first workout" CTA present
    3. Click CTA — should navigate to /workout
    4. Log a quick workout: add any exercise, log 1 set (e.g., 10 reps x 50kg), finish session
    5. Navigate back to /ranking
    6. Verify: radar chart shows data on at least one axis, tier shows "Iron", progress bar shows movement
    7. Verify weekly summary shows "1 session this week" and volume
    8. Log several more sets across different exercise types to accumulate > 5000 kg total volume
    9. Return to /ranking — verify tier advances to Bronze and celebration overlay appears with confetti
    10. Verify celebration auto-dismisses after ~3 seconds OR click "Continue"
    11. Refresh page — celebration should NOT re-appear (lastSeenTier persisted)
    12. Check BottomNav has 5 tabs: Map, Exercises, History, Ranking, Workout
    13. Check mobile view (narrow viewport): all elements stack properly, radar chart is 280px
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- All E2E tests pass
- Human confirms visual quality of all ranking elements
- Celebration fires exactly once per tier advance
</verification>

<success_criteria>
All RANK requirements verified by automated E2E tests and human visual inspection. Ranking page is ship-ready.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ranking-radar/07-03-SUMMARY.md`
</output>
