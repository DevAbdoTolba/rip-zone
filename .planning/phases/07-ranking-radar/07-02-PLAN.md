---
phase: 07-ranking-radar
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/app/(main)/ranking/page.tsx
  - src/components/ranking/RadarChart.tsx
  - src/components/ranking/TierBadge.tsx
  - src/components/ranking/ProgressBar.tsx
  - src/components/ranking/CelebrationOverlay.tsx
  - src/components/ranking/RankingDashboard.tsx
  - src/components/bottom-nav/BottomNav.tsx
autonomous: true
requirements: [RANK-01, RANK-02, RANK-03, RANK-04]

must_haves:
  truths:
    - "User sees radar chart as hero element at top of /ranking page"
    - "User sees tier badge with current rank name below radar chart"
    - "User sees sub-tier progress bar showing advancement toward next tier"
    - "User sees last 7 days summary (sessions + volume) for quick context"
    - "User sees celebration overlay on first visit after tier advance"
    - "User with zero workouts sees empty state with radar outline at 0 and CTA"
    - "Bottom nav has 5 tabs including Ranking"
  artifacts:
    - path: "src/components/ranking/RadarChart.tsx"
      provides: "Inline SVG 5-axis radar chart"
      contains: "polygon"
    - path: "src/components/ranking/TierBadge.tsx"
      provides: "Tier name + icon display"
      contains: "TierRank"
    - path: "src/components/ranking/ProgressBar.tsx"
      provides: "Sub-tier progress bar"
      contains: "subTierProgress"
    - path: "src/components/ranking/CelebrationOverlay.tsx"
      provides: "Full-screen tier-up celebration"
      contains: "confetti"
    - path: "src/components/ranking/RankingDashboard.tsx"
      provides: "Client component orchestrating all ranking UI"
      contains: "useRankingData"
    - path: "src/app/(main)/ranking/page.tsx"
      provides: "Ranking page route"
      contains: "RankingDashboard"
    - path: "src/components/bottom-nav/BottomNav.tsx"
      provides: "Updated 5-tab nav"
      contains: "/ranking"
  key_links:
    - from: "src/components/ranking/RankingDashboard.tsx"
      to: "src/hooks/useRankingData.ts"
      via: "hook call"
      pattern: "useRankingData"
    - from: "src/components/ranking/CelebrationOverlay.tsx"
      to: "src/lib/db/workouts.ts"
      via: "lastSeenTier comparison"
      pattern: "lastSeenTier"
    - from: "src/components/bottom-nav/BottomNav.tsx"
      to: "/ranking"
      via: "nav tab link"
      pattern: "href.*ranking"
---

<objective>
Build the complete ranking page UI: inline SVG radar chart, tier badge, sub-tier progress bar, celebration overlay, empty state, and BottomNav integration.

Purpose: Delivers all four RANK requirements as a visible, interactive ranking screen.
Output: /ranking page with radar chart, tier display, progress bar, celebration moment.
</objective>

<execution_context>
@/home/appuser/workspace/rip-zone/.claude/get-shit-done/workflows/execute-plan.md
@/home/appuser/workspace/rip-zone/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-ranking-radar/07-CONTEXT.md
@.planning/phases/07-ranking-radar/07-01-SUMMARY.md

<interfaces>
From src/hooks/useRankingData.ts (created in Plan 01):
```typescript
export function useRankingData(): {
  tier: TierRank
  subTierProgress: number
  radarAxes: { push: number; pull: number; legs: number; core: number; conditioning: number }
  totalVolume: number
  weekSummary: { sessions: number; volume: number }
  lastSeenTier: TierRank | null
  setLastSeenTier: (tier: TierRank) => Promise<void>
  isLoading: boolean
}
```

From src/lib/ranking.ts (created in Plan 01):
```typescript
export const TIER_THRESHOLDS: { tier: TierRank; minVolume: number }[]
export function computeTierRank(totalVolume: number): TierRank
export function computeSubTierProgress(totalVolume: number): number
export function computeRadarAxes(categoryVolumes: Record<string, number>, tierMaxVolume: number): RadarAxes
export const CATEGORY_MAP: Record<string, string>
```

From src/components/bottom-nav/BottomNav.tsx:
```typescript
const tabs = [
  { href: '/', label: 'Map', icon: MapPin },
  { href: '/exercises', label: 'Exercises', icon: Dumbbell },
  { href: '/history', label: 'History', icon: History },
  { href: '/workout', label: 'Workout', icon: Timer },
]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Radar chart, tier badge, progress bar, and celebration overlay components</name>
  <files>src/components/ranking/RadarChart.tsx, src/components/ranking/TierBadge.tsx, src/components/ranking/ProgressBar.tsx, src/components/ranking/CelebrationOverlay.tsx</files>
  <read_first>src/types/ranking.ts, src/lib/ranking.ts, src/app/globals.css, src/components/bottom-nav/BottomNav.tsx</read_first>
  <action>
    1. Create src/components/ranking/RadarChart.tsx ('use client'):
       - Props: { axes: { push: number; pull: number; legs: number; core: number; conditioning: number } }
       - Inline SVG, viewBox="0 0 300 300", center at (150, 150)
       - 5 axes at 72-degree intervals starting from top (270deg / -90deg)
       - Draw 3 concentric pentagons (grid lines) at 33%, 66%, 100% radius in stroke muted-foreground/30
       - Draw axis lines from center to each vertex in stroke muted-foreground/50
       - Draw data polygon: vertices at radius = (axisValue / 100) * maxRadius (maxRadius = 120)
       - Data polygon: fill oklch(var(--primary) / 0.2), stroke oklch(var(--primary)), strokeWidth 2
       - Axis labels outside polygon: Push (top), Pull (top-right), Legs (bottom-right), Core (bottom-left), Conditioning (top-left)
       - Labels in fill text-foreground, font-size 12px
       - Responsive: w-[280px] h-[280px] md:w-[320px] md:h-[320px] via className on wrapper div
       - When all axes are 0, still render the grid lines and labels (empty state visual)

    2. Create src/components/ranking/TierBadge.tsx ('use client'):
       - Props: { tier: TierRank, totalVolume: number }
       - Display tier name in large text (text-2xl font-bold)
       - Color per tier: Iron=#8B8B8B, Bronze=#CD7F32, Silver=#C0C0C0, Gold=#FFD700, Platinum=#E5E4E2, Diamond=#B9F2FF, Elite=#FF4500
       - Show total volume formatted with locale string (e.g., "125,430 kg lifetime")
       - Use a shield or trophy icon from lucide-react sized 32px, colored per tier

    3. Create src/components/ranking/ProgressBar.tsx ('use client'):
       - Props: { progress: number (0-1), currentTier: TierRank, nextTier: TierRank | null }
       - Horizontal bar: h-3 rounded-full bg-muted, inner fill with bg-primary and rounded-full
       - Width = progress * 100%
       - Labels: current tier name on left, next tier name on right (or "MAX" if Elite)
       - Percentage in center of bar if > 15% progress
       - Animate width with transition-all duration-500

    4. Create src/components/ranking/CelebrationOverlay.tsx ('use client'):
       - Props: { newTier: TierRank, onDismiss: () => void }
       - Fixed inset-0 z-[100] bg-black/80 backdrop-blur-sm flex items-center justify-center
       - Center card: tier name, "You reached {tier}!" message, tier-colored badge icon
       - CSS-only confetti: 20-30 small divs with @keyframes confetti-fall animation
         - Random horizontal positions, random delays (0-2s), fall duration 2-3s
         - Colors: mix of primary, gold, white, tier color
         - Animation: translateY(-100vh) to translateY(100vh) with rotate and slight horizontal drift
       - "Continue" button (bg-primary text-primary-foreground rounded-lg px-6 py-3)
       - Auto-dismiss after 3 seconds via setTimeout calling onDismiss
       - Add the @keyframes confetti-fall to a <style> tag inside the component or use Tailwind arbitrary animation
  </action>
  <verify>
    <automated>cd /home/appuser/workspace/rip-zone && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "polygon" src/components/ranking/RadarChart.tsx
    - grep -q "'use client'" src/components/ranking/RadarChart.tsx
    - grep -q "TierRank" src/components/ranking/TierBadge.tsx
    - grep -q "subTierProgress\|progress" src/components/ranking/ProgressBar.tsx
    - grep -q "confetti" src/components/ranking/CelebrationOverlay.tsx
    - grep -q "onDismiss" src/components/ranking/CelebrationOverlay.tsx
    - grep -q "Continue" src/components/ranking/CelebrationOverlay.tsx
    - TypeScript compiles with no errors
  </acceptance_criteria>
  <done>Four ranking components render correctly: RadarChart draws 5-axis SVG polygon, TierBadge shows colored tier, ProgressBar shows animated fill, CelebrationOverlay shows confetti with auto-dismiss.</done>
</task>

<task type="auto">
  <name>Task 2: RankingDashboard, /ranking page, and BottomNav update</name>
  <files>src/components/ranking/RankingDashboard.tsx, src/app/(main)/ranking/page.tsx, src/components/bottom-nav/BottomNav.tsx</files>
  <read_first>src/hooks/useRankingData.ts, src/components/ranking/RadarChart.tsx, src/components/ranking/TierBadge.tsx, src/components/ranking/ProgressBar.tsx, src/components/ranking/CelebrationOverlay.tsx, src/components/bottom-nav/BottomNav.tsx, src/app/(main)/layout.tsx, src/lib/ranking.ts</read_first>
  <action>
    1. Create src/components/ranking/RankingDashboard.tsx ('use client'):
       - Import and call useRankingData()
       - Loading state: skeleton placeholder (animate-pulse divs matching layout)
       - Empty state (totalVolume === 0):
         - RadarChart with all axes at 0 (shows grid outline)
         - Text: "Log your first workout to start ranking"
         - Link to /workout as CTA button (bg-primary text-primary-foreground rounded-lg px-4 py-2)
       - Normal state layout (single column, centered):
         a. Week summary card at top: "{weekSummary.sessions} sessions this week / {weekSummary.volume.toLocaleString()} kg"
            - Use a subtle bg-card rounded-xl p-4 border border-border
         b. RadarChart as hero (centered, largest element)
         c. TierBadge below chart
         d. ProgressBar below badge (max-w-sm mx-auto)
       - Celebration logic:
         - On mount, if !isLoading && lastSeenTier !== null && lastSeenTier !== tier.toString():
           set showCelebration = true
         - On first ever visit (lastSeenTier === null): call setLastSeenTier(tier) silently, no celebration
         - CelebrationOverlay onDismiss: setShowCelebration(false), call setLastSeenTier(tier)
       - Import TIER_THRESHOLDS from '@/lib/ranking' to derive nextTier name for ProgressBar

    2. Create src/app/(main)/ranking/page.tsx:
       - Default export Server Component
       - Import RankingDashboard
       - Layout: div with px-4 py-6, max-w-lg mx-auto
       - h1 "Ranking" in text-xl font-semibold text-foreground mb-4
       - Render <RankingDashboard />
       - Add pb-20 for mobile bottom nav clearance

    3. Update src/components/bottom-nav/BottomNav.tsx:
       - Import Trophy from lucide-react
       - Add { href: '/ranking', label: 'Ranking', icon: Trophy } to tabs array — insert between History and Workout:
         Final order: Map, Exercises, History, Ranking, Workout
       - No other changes to BottomNav structure
  </action>
  <verify>
    <automated>cd /home/appuser/workspace/rip-zone && npx tsc --noEmit --pretty 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "useRankingData" src/components/ranking/RankingDashboard.tsx
    - grep -q "CelebrationOverlay" src/components/ranking/RankingDashboard.tsx
    - grep -q "RadarChart" src/components/ranking/RankingDashboard.tsx
    - grep -q "Log your first workout" src/components/ranking/RankingDashboard.tsx
    - grep -q "RankingDashboard" src/app/(main)/ranking/page.tsx
    - grep -q "/ranking" src/components/bottom-nav/BottomNav.tsx
    - grep -q "Trophy" src/components/bottom-nav/BottomNav.tsx
    - TypeScript compiles with no errors
  </acceptance_criteria>
  <done>/ranking page renders with radar chart hero, tier badge, progress bar, week summary, empty state, and celebration overlay. BottomNav shows 5 tabs including Ranking.</done>
</task>

</tasks>

<verification>
- TypeScript compiles with no errors
- /ranking page renders server-side without hydration errors
- BottomNav shows 5 tabs
- Empty state shows when no workout data exists
</verification>

<success_criteria>
User can navigate to /ranking via bottom nav, see radar chart, tier badge, progress bar, weekly summary, and celebration overlay triggers on tier advancement. Zero-workout empty state shows CTA to log first workout.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ranking-radar/07-02-SUMMARY.md`
</output>
