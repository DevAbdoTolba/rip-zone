---
phase: 03-exercise-library
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/warmups.json
  - src/models/Warmup.ts
  - scripts/seed.ts
  - src/components/bottom-nav/BottomNav.tsx
  - src/app/(main)/layout.tsx
  - src/app/(main)/page.tsx
  - src/app/page.tsx
  - src/lib/exercise-filter.ts
  - tests/lib/exercise-filter.test.ts
  - e2e/exercise-library.spec.ts
autonomous: true
requirements:
  - EXER-03
must_haves:
  truths:
    - "Bottom navigation shell renders with Map and Exercises tabs on both / and /exercises routes"
    - "Muscle map page still accessible at / (URL unchanged after route group migration)"
    - "Warm-up seed data exists with 9 muscle groups, each having 3-5 movements"
    - "Pure filter function correctly filters exercises by name, equipment, and muscle"
  artifacts:
    - path: "data/warmups.json"
      provides: "Warm-up seed data for 9 muscle groups"
      contains: "muscleGroup"
    - path: "src/models/Warmup.ts"
      provides: "Mongoose model for warm-up data"
      contains: "WarmupSchema"
    - path: "src/components/bottom-nav/BottomNav.tsx"
      provides: "Bottom navigation with Map and Exercises tabs"
      contains: "BottomNav"
    - path: "src/app/(main)/layout.tsx"
      provides: "Route group layout with bottom nav shell"
      contains: "BottomNav"
    - path: "src/app/(main)/page.tsx"
      provides: "Muscle map page relocated into route group"
      contains: "MuscleMap"
    - path: "src/lib/exercise-filter.ts"
      provides: "Pure filter function for exercises"
      exports: ["filterExercises"]
    - path: "tests/lib/exercise-filter.test.ts"
      provides: "Unit tests for filter logic"
      min_lines: 40
    - path: "e2e/exercise-library.spec.ts"
      provides: "E2E test scaffolds for EXER-01, EXER-02, EXER-03"
      min_lines: 20
  key_links:
    - from: "src/app/(main)/layout.tsx"
      to: "src/components/bottom-nav/BottomNav.tsx"
      via: "import and render"
      pattern: "import.*BottomNav"
    - from: "scripts/seed.ts"
      to: "data/warmups.json"
      via: "readFileSync"
      pattern: "warmups\\.json"
---

<objective>
Infrastructure, data, and test scaffolds for the Exercise Library phase.

Purpose: Establishes the route group layout with bottom navigation, creates warm-up seed data and Mongoose model, extracts the pure filter function with TDD, and creates test scaffolds so all subsequent plans have a runnable verification baseline.

Output: Route group migration complete, bottom nav rendering, warm-up data seeded, filter function tested, E2E stubs ready.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-exercise-library/03-CONTEXT.md
@.planning/phases/03-exercise-library/03-RESEARCH.md
@.planning/phases/03-exercise-library/03-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/models/Exercise.ts:
```typescript
const ExerciseSchema = new Schema({
  slug: { type: String, required: true, unique: true },
  name: { type: String, required: true },
  primaryMuscles: { type: [String], required: true },
  secondaryMuscles: { type: [String], default: [] },
  equipment: {
    type: String, required: true,
    enum: ['barbell', 'dumbbell', 'cable', 'machine', 'bodyweight', 'kettlebell', 'band', 'other'],
  },
  difficulty: {
    type: String, required: true,
    enum: ['beginner', 'intermediate', 'advanced'],
  },
  description: { type: String, required: true },
  formCues: { type: [String], default: [] },
})
export type ExerciseDocument = InferSchemaType<typeof ExerciseSchema>
```

From src/models/Muscle.ts:
```typescript
const MuscleSchema = new Schema({
  slug: { type: String, required: true, unique: true },
  displayName: { type: String, required: true },
  group: {
    type: String, required: true,
    enum: ['chest', 'back', 'shoulders', 'arms', 'forearms', 'core', 'legs', 'glutes', 'calves'],
  },
  svgRegion: { type: String, required: true },
})
export type MuscleDocument = InferSchemaType<typeof MuscleSchema>
```

From src/types/muscle.ts:
```typescript
export type MuscleGroup = 'chest' | 'back' | 'shoulders' | 'arms' | 'forearms' | 'core' | 'legs' | 'glutes' | 'calves'
```

From src/app/page.tsx (current — will be moved):
```typescript
import { MuscleMap } from '@/components/muscle-map/MuscleMap'
export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center px-8 py-12">
      <h1 className="text-[20px] font-semibold text-foreground mb-6">Rip Zone</h1>
      <MuscleMap />
    </main>
  )
}
```

From src/app/layout.tsx (root — must NOT be modified):
```typescript
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className={inter.variable}>
      <body className="bg-background text-foreground font-sans antialiased">
        {children}
      </body>
    </html>
  )
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Warm-up data, Mongoose model, seed script update, and pure filter function with TDD</name>
  <files>
    data/warmups.json,
    src/models/Warmup.ts,
    scripts/seed.ts,
    src/lib/exercise-filter.ts,
    tests/lib/exercise-filter.test.ts,
    e2e/exercise-library.spec.ts
  </files>
  <read_first>
    src/models/Exercise.ts,
    src/models/Muscle.ts,
    src/types/muscle.ts,
    scripts/seed.ts,
    data/exercises.json,
    data/muscles.json,
    e2e/muscle-map.spec.ts,
    tests/setup.ts,
    vitest.config.ts,
    playwright.config.ts
  </read_first>
  <behavior>
    - filterExercises({exercises, query: 'bench'}) returns only exercises whose name contains 'bench' (case-insensitive)
    - filterExercises({exercises, equipment: 'barbell'}) returns only exercises with equipment === 'barbell'
    - filterExercises({exercises, muscle: 'pectoralis-major'}) returns only exercises where primaryMuscles includes 'pectoralis-major'
    - filterExercises({exercises, query: 'bench', equipment: 'barbell'}) applies both filters (AND logic)
    - filterExercises({exercises}) with no filters returns all exercises unchanged
    - filterExercises({exercises, query: 'xyznonexistent'}) returns empty array
  </behavior>
  <action>
    **1. Create `data/warmups.json`** — 9 muscle groups, each with 3-5 warm-up movements. Structure:
    ```json
    [
      {
        "muscleGroup": "chest",
        "movements": [
          { "name": "Arm Circles", "instruction": "Stand tall and make large forward circles with both arms to loosen the shoulder joints and warm up the chest.", "duration": "30 seconds" },
          { "name": "Band Pull-Aparts", "instruction": "Hold a resistance band at shoulder width and pull apart horizontally, squeezing your shoulder blades.", "duration": "15 reps" },
          { "name": "Incline Push-ups", "instruction": "Place hands on an elevated surface and perform slow push-ups to activate the pecs with minimal load.", "duration": "10 reps" },
          { "name": "Dynamic Chest Stretch", "instruction": "Stand in a doorway with arms at 90 degrees on the frame, lean forward gently and hold, then release and repeat.", "duration": "20 seconds each side" }
        ]
      },
      {
        "muscleGroup": "back",
        "movements": [
          { "name": "Cat-Cow Stretch", "instruction": "On hands and knees, alternate between arching your back up (cat) and dropping your belly down (cow).", "duration": "10 reps" },
          { "name": "Band Rows", "instruction": "Loop a resistance band around a sturdy pole, step back for tension, and row both hands to your ribcage.", "duration": "15 reps" },
          { "name": "Scapular Pull-ups", "instruction": "Hang from a bar and retract your shoulder blades without bending your elbows — pure scapular movement.", "duration": "10 reps" },
          { "name": "Thoracic Rotation", "instruction": "On all fours, place one hand behind your head and rotate your upper body toward the ceiling, then back down.", "duration": "10 reps each side" }
        ]
      },
      {
        "muscleGroup": "shoulders",
        "movements": [
          { "name": "Arm Circles", "instruction": "Make small to large circles with your arms extended, warming up the rotator cuff and deltoids.", "duration": "30 seconds each direction" },
          { "name": "Band Dislocates", "instruction": "Hold a resistance band wide and pass it over and behind your head in a full arc, then reverse.", "duration": "10 reps" },
          { "name": "Wall Slides", "instruction": "Stand with your back against a wall, arms in a 'W' shape, and slide them up to a 'Y' and back down.", "duration": "10 reps" },
          { "name": "Light Lateral Raises", "instruction": "With very light dumbbells or no weight, raise arms to the side to shoulder height and lower slowly.", "duration": "15 reps" }
        ]
      },
      {
        "muscleGroup": "arms",
        "movements": [
          { "name": "Wrist Circles", "instruction": "Extend your arms and rotate your wrists in circles to warm up the forearm and bicep tendons.", "duration": "20 seconds each direction" },
          { "name": "Light Curls", "instruction": "Grab the lightest dumbbells available and perform slow, controlled curls for blood flow.", "duration": "15 reps" },
          { "name": "Tricep Stretch", "instruction": "Raise one arm overhead, bend at the elbow, and use the other hand to gently press the elbow back.", "duration": "20 seconds each arm" }
        ]
      },
      {
        "muscleGroup": "forearms",
        "movements": [
          { "name": "Wrist Flexor Stretch", "instruction": "Extend your arm with palm up, use the other hand to gently pull fingers back toward you.", "duration": "20 seconds each arm" },
          { "name": "Wrist Extensor Stretch", "instruction": "Extend your arm with palm down, use the other hand to gently press the back of the hand toward you.", "duration": "20 seconds each arm" },
          { "name": "Grip Squeezes", "instruction": "Rapidly squeeze and release a soft ball or make tight fists to pump blood into the forearms.", "duration": "30 seconds" }
        ]
      },
      {
        "muscleGroup": "core",
        "movements": [
          { "name": "Dead Bug", "instruction": "Lie on your back with arms extended up and knees at 90 degrees. Alternate extending opposite arm and leg while keeping your lower back pressed to the floor.", "duration": "10 reps each side" },
          { "name": "Plank Hold", "instruction": "Hold a forearm plank with a straight line from head to heels, bracing your core.", "duration": "30 seconds" },
          { "name": "Cat-Cow", "instruction": "On all fours, alternate between rounding your spine up and arching it down to mobilize the core.", "duration": "10 reps" },
          { "name": "Standing Oblique Stretch", "instruction": "Stand tall, reach one arm overhead and lean to the opposite side, stretching the obliques.", "duration": "20 seconds each side" }
        ]
      },
      {
        "muscleGroup": "legs",
        "movements": [
          { "name": "Bodyweight Squats", "instruction": "Perform slow, controlled bodyweight squats through full range of motion to warm up quads, glutes, and hips.", "duration": "15 reps" },
          { "name": "Leg Swings (Front-to-Back)", "instruction": "Hold a wall for balance and swing one leg forward and backward in a controlled arc.", "duration": "15 swings each leg" },
          { "name": "Leg Swings (Side-to-Side)", "instruction": "Face the wall, hold for balance, and swing one leg across your body and out to the side.", "duration": "15 swings each leg" },
          { "name": "Walking Lunges", "instruction": "Take exaggerated steps forward, dropping your back knee toward the floor with each step.", "duration": "10 steps each leg" },
          { "name": "Ankle Circles", "instruction": "Lift one foot and rotate the ankle in circles to warm up the ankle joint.", "duration": "15 circles each direction" }
        ]
      },
      {
        "muscleGroup": "glutes",
        "movements": [
          { "name": "Glute Bridges", "instruction": "Lie on your back with knees bent, drive through your heels to lift your hips and squeeze your glutes at the top.", "duration": "15 reps" },
          { "name": "Clamshells", "instruction": "Lie on your side with knees bent, keep feet together and open your top knee like a clamshell.", "duration": "15 reps each side" },
          { "name": "Fire Hydrants", "instruction": "On all fours, lift one knee out to the side while keeping it bent at 90 degrees.", "duration": "12 reps each side" },
          { "name": "Hip Circles", "instruction": "Stand on one leg and make large circles with the other knee to open up the hip joint.", "duration": "10 circles each direction" }
        ]
      },
      {
        "muscleGroup": "calves",
        "movements": [
          { "name": "Calf Raises", "instruction": "Stand on the edge of a step, lower your heels below the platform, then rise up onto your toes.", "duration": "20 reps" },
          { "name": "Wall Calf Stretch", "instruction": "Place hands on a wall, step one foot back, and press the heel into the floor with a straight leg.", "duration": "20 seconds each leg" },
          { "name": "Ankle Circles", "instruction": "Lift one foot off the ground and rotate the ankle in full circles.", "duration": "15 circles each direction" }
        ]
      }
    ]
    ```

    **2. Create `src/models/Warmup.ts`** — Mongoose model following exact project pattern (HMR guard):
    ```typescript
    import mongoose, { Schema, type InferSchemaType } from 'mongoose'

    const WarmupMovementSchema = new Schema({
      name: { type: String, required: true },
      instruction: { type: String, required: true },
      duration: { type: String, required: true },
    })

    const WarmupSchema = new Schema({
      muscleGroup: {
        type: String,
        required: true,
        enum: ['chest', 'back', 'shoulders', 'arms', 'forearms', 'core', 'legs', 'glutes', 'calves'],
        unique: true,
      },
      movements: { type: [WarmupMovementSchema], required: true },
    })

    export type WarmupDocument = InferSchemaType<typeof WarmupSchema>
    const Warmup = mongoose.models.Warmup ?? mongoose.model('Warmup', WarmupSchema)
    export default Warmup
    ```

    **3. Update `scripts/seed.ts`** — Add Warmup import and seeding. After the FaqEntry seeding block, add:
    ```typescript
    const { default: Warmup } = await import('../src/models/Warmup')
    // in load JSON section:
    const warmupsData = JSON.parse(readFileSync(path.resolve(process.cwd(), 'data/warmups.json'), 'utf-8'))
    // in seed section:
    await Warmup.deleteMany({})
    await Warmup.insertMany(warmupsData)
    console.log(`Seeded ${warmupsData.length} warmup guides`)
    ```

    **4. Create `src/lib/exercise-filter.ts`** — Pure filter function:
    ```typescript
    export interface ExerciseFilterParams {
      exercises: Array<{
        name: string
        equipment: string
        primaryMuscles: string[]
      }>
      query?: string
      equipment?: string
      muscle?: string
    }

    export function filterExercises({ exercises, query, equipment, muscle }: ExerciseFilterParams) {
      return exercises.filter(ex => {
        const matchesQuery = !query || ex.name.toLowerCase().includes(query.toLowerCase())
        const matchesEquipment = !equipment || ex.equipment === equipment
        const matchesMuscle = !muscle || ex.primaryMuscles.includes(muscle)
        return matchesQuery && matchesEquipment && matchesMuscle
      })
    }
    ```

    **5. Create `tests/lib/exercise-filter.test.ts`** — TDD: write tests first, then the implementation above.
    Test cases:
    - Filters by name substring (case-insensitive): `filterExercises({exercises: mockData, query: 'bench'})` returns 4+ exercises containing "bench"
    - Filters by equipment: `filterExercises({exercises: mockData, equipment: 'barbell'})` returns only barbell exercises
    - Filters by muscle: `filterExercises({exercises: mockData, muscle: 'pectoralis-major'})` returns exercises targeting that muscle
    - Combines filters (AND): `filterExercises({exercises: mockData, query: 'bench', equipment: 'barbell'})` returns intersection
    - No filters returns all: `filterExercises({exercises: mockData})` returns full array
    - No match returns empty: `filterExercises({exercises: mockData, query: 'xyznonexistent'})` returns `[]`
    Use a small mock dataset of 5-6 exercises covering different equipment/difficulty/muscles, imported or defined inline.

    **6. Create `e2e/exercise-library.spec.ts`** — E2E test stubs. Create the file with `test.describe('Exercise Library')` containing:
    - `test('EXER-01: displays exercises grouped by muscle group')` — navigates to `/exercises`, expects heading "Exercises" visible, expects at least one muscle group section heading (e.g., "Chest"), expects exercise card visible
    - `test('EXER-02: search filters exercises by name')` — fills search input with "bench", expects only exercises containing "bench" visible
    - `test('EXER-02: equipment filter works')` — clicks "Barbell" chip, expects visible exercises to have barbell equipment
    - `test('EXER-03: warm-up sheet opens for muscle group')` — clicks "View warm-up" badge, expects bottom sheet with warm-up movements
    All tests should be real Playwright tests (not `.skip`) — they will fail until Plan 02 and 03 build the UI, but the file provides the verification target. Use `page.goto('/exercises')` and Playwright locators.
  </action>
  <verify>
    <automated>cd /home/appuser/workspace/rip-zone && npx vitest run tests/lib/exercise-filter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - data/warmups.json contains 9 objects, each with "muscleGroup" key matching one of: chest, back, shoulders, arms, forearms, core, legs, glutes, calves
    - data/warmups.json: each object has "movements" array with 3-5 items, each having "name", "instruction", "duration" keys
    - src/models/Warmup.ts contains `mongoose.models.Warmup ?? mongoose.model('Warmup', WarmupSchema)`
    - src/models/Warmup.ts contains `export type WarmupDocument`
    - src/models/Warmup.ts contains `enum: ['chest', 'back', 'shoulders', 'arms', 'forearms', 'core', 'legs', 'glutes', 'calves']`
    - scripts/seed.ts contains `import('../src/models/Warmup')`
    - scripts/seed.ts contains `warmups.json`
    - scripts/seed.ts contains `Warmup.deleteMany` and `Warmup.insertMany`
    - src/lib/exercise-filter.ts contains `export function filterExercises`
    - src/lib/exercise-filter.ts contains `ex.name.toLowerCase().includes(query.toLowerCase())`
    - src/lib/exercise-filter.ts contains `ex.equipment === equipment`
    - src/lib/exercise-filter.ts contains `ex.primaryMuscles.includes(muscle)`
    - tests/lib/exercise-filter.test.ts exits 0 with all tests passing
    - tests/lib/exercise-filter.test.ts contains at least 5 `it(` or `test(` calls
    - e2e/exercise-library.spec.ts contains `page.goto('/exercises')`
    - e2e/exercise-library.spec.ts contains test names referencing EXER-01, EXER-02, EXER-03
  </acceptance_criteria>
  <done>
    Warm-up seed data ready (9 groups, 3-5 movements each). Warmup Mongoose model exists with HMR guard. Seed script updated to seed warmups. Pure filter function passes 6+ unit tests. E2E test scaffold file exists with real test bodies for all 3 requirements.
  </done>
</task>

<task type="auto">
  <name>Task 2: Route group migration and bottom navigation shell</name>
  <files>
    src/app/(main)/layout.tsx,
    src/app/(main)/page.tsx,
    src/app/page.tsx,
    src/components/bottom-nav/BottomNav.tsx
  </files>
  <read_first>
    src/app/page.tsx,
    src/app/layout.tsx,
    src/app/globals.css,
    src/components/muscle-map/MuscleMap.tsx,
    e2e/smoke.spec.ts,
    e2e/muscle-map.spec.ts,
    .planning/phases/03-exercise-library/03-UI-SPEC.md
  </read_first>
  <action>
    **1. Create `src/components/bottom-nav/BottomNav.tsx`** — Client component with responsive bottom/top nav. Per D-06 and UI-SPEC:
    ```typescript
    'use client'

    import { usePathname } from 'next/navigation'
    import Link from 'next/link'
    import { MapPin, Dumbbell } from 'lucide-react'
    import { cn } from '@/lib/utils'

    const tabs = [
      { href: '/', label: 'Map', icon: MapPin },
      { href: '/exercises', label: 'Exercises', icon: Dumbbell },
    ]

    export function BottomNav() {
      const pathname = usePathname()

      return (
        <>
          {/* Mobile: fixed bottom bar */}
          <nav className="fixed bottom-0 left-0 right-0 z-50 flex md:hidden h-16 items-center justify-around border-t border-border bg-card" aria-label="Main navigation">
            {tabs.map(({ href, label, icon: Icon }) => {
              const isActive = href === '/' ? pathname === '/' : pathname.startsWith(href)
              return (
                <Link
                  key={href}
                  href={href}
                  className={cn(
                    'flex flex-col items-center justify-center gap-1 min-h-[44px] min-w-[44px] px-3 py-2 text-[14px] font-normal transition-colors',
                    isActive ? 'text-primary' : 'text-muted-foreground'
                  )}
                  aria-current={isActive ? 'page' : undefined}
                >
                  <Icon size={24} />
                  <span>{label}</span>
                </Link>
              )
            })}
          </nav>
          {/* Desktop: horizontal tab nav at top */}
          <nav className="hidden md:flex items-center gap-1 border-b border-border bg-card px-8 h-12" aria-label="Main navigation">
            {tabs.map(({ href, label, icon: Icon }) => {
              const isActive = href === '/' ? pathname === '/' : pathname.startsWith(href)
              return (
                <Link
                  key={href}
                  href={href}
                  className={cn(
                    'flex items-center gap-2 px-4 py-2 text-[14px] font-normal rounded-lg transition-colors',
                    isActive
                      ? 'text-primary bg-primary/10'
                      : 'text-muted-foreground hover:text-foreground hover:bg-muted'
                  )}
                  aria-current={isActive ? 'page' : undefined}
                >
                  <Icon size={20} />
                  <span>{label}</span>
                </Link>
              )
            })}
          </nav>
        </>
      )
    }
    ```

    **2. Create `src/app/(main)/layout.tsx`** — Route group layout wrapping content with bottom nav:
    ```typescript
    import { BottomNav } from '@/components/bottom-nav/BottomNav'

    export default function MainLayout({ children }: { children: React.ReactNode }) {
      return (
        <div className="flex flex-col min-h-dvh">
          {/* Desktop nav at top */}
          <div className="hidden md:block">
            <BottomNav />
          </div>
          <div className="flex-1 pb-16 md:pb-0">{children}</div>
          {/* Mobile nav at bottom (rendered by BottomNav, positioned fixed) */}
          <div className="md:hidden">
            <BottomNav />
          </div>
        </div>
      )
    }
    ```
    NOTE: The BottomNav component renders both mobile and desktop variants internally using responsive classes. The layout wraps it for structural purposes. Actually, since BottomNav internally shows/hides via `md:hidden` / `hidden md:flex`, the layout should simply render BottomNav once:
    ```typescript
    import { BottomNav } from '@/components/bottom-nav/BottomNav'

    export default function MainLayout({ children }: { children: React.ReactNode }) {
      return (
        <>
          <BottomNav />
          <div className="flex-1 pb-16 md:pb-0 md:pt-0">{children}</div>
        </>
      )
    }
    ```
    The BottomNav desktop variant (hidden md:flex) uses border-b and sits in normal flow at the top of the DOM. The mobile variant (fixed bottom-0) overlays. The `pb-16` on the content div ensures cards are not hidden behind the mobile bottom nav. On desktop `md:pb-0` removes the padding since the nav is at the top.

    **3. Move `src/app/page.tsx` to `src/app/(main)/page.tsx`** — Move the file (git mv or copy contents). The content stays the same:
    ```typescript
    import { MuscleMap } from '@/components/muscle-map/MuscleMap'

    export default function Home() {
      return (
        <main className="flex min-h-screen flex-col items-center px-8 py-12">
          <h1 className="text-[20px] font-semibold text-foreground mb-6">Rip Zone</h1>
          <MuscleMap />
        </main>
      )
    }
    ```
    After the move, the old `src/app/page.tsx` must be deleted. The URL `/` still works because route groups `(main)` are transparent to URLs.

    **4. Verify existing E2E tests still pass** — The smoke test and muscle map test navigate to `/` which must still resolve after route group migration. Run E2E tests to confirm.
  </action>
  <verify>
    <automated>cd /home/appuser/workspace/rip-zone && npx playwright test e2e/smoke.spec.ts e2e/muscle-map.spec.ts --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - src/app/page.tsx does NOT exist (deleted after move)
    - src/app/(main)/page.tsx contains `import { MuscleMap }`
    - src/app/(main)/layout.tsx contains `import { BottomNav }`
    - src/app/(main)/layout.tsx contains `pb-16`
    - src/components/bottom-nav/BottomNav.tsx contains `'use client'`
    - src/components/bottom-nav/BottomNav.tsx contains `usePathname`
    - src/components/bottom-nav/BottomNav.tsx contains `MapPin` and `Dumbbell` from lucide-react
    - src/components/bottom-nav/BottomNav.tsx contains `href: '/'` and `href: '/exercises'`
    - src/components/bottom-nav/BottomNav.tsx contains `min-h-[44px]` (touch target per WCAG)
    - src/components/bottom-nav/BottomNav.tsx contains `text-primary` for active state
    - src/components/bottom-nav/BottomNav.tsx contains `text-muted-foreground` for inactive state
    - src/components/bottom-nav/BottomNav.tsx contains `h-16` (64px bottom nav height)
    - src/components/bottom-nav/BottomNav.tsx contains `aria-label="Main navigation"`
    - Existing E2E smoke and muscle-map tests pass (/ still resolves)
  </acceptance_criteria>
  <done>
    Bottom navigation shell renders on all (main) route group pages. Muscle map page accessible at / with URL unchanged. Mobile shows fixed bottom bar (64px), desktop shows horizontal tab nav at top. Existing E2E tests pass confirming no regression from route group migration.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run tests/lib/exercise-filter.test.ts` — all unit tests pass
- `npx playwright test e2e/smoke.spec.ts e2e/muscle-map.spec.ts` — existing E2E tests pass after migration
- `src/app/page.tsx` does not exist (moved to route group)
- `data/warmups.json` has 9 entries with muscleGroup matching the 9 known groups
</verification>

<success_criteria>
- Route group `(main)` established with bottom nav shell
- Muscle map page still renders at `/` (no URL change)
- Bottom nav shows Map and Exercises tabs with correct active states
- Warmup Mongoose model and seed data ready for Phase 3 Plan 03
- Pure filter function tested with 6+ unit tests passing
- E2E test scaffold created for EXER-01, EXER-02, EXER-03
</success_criteria>

<output>
After completion, create `.planning/phases/03-exercise-library/03-01-SUMMARY.md`
</output>
