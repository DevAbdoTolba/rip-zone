---
phase: 04-workout-logger
plan: 06
type: execute
wave: 3
depends_on: ["04-03", "04-04", "04-05"]
files_modified:
  - e2e/workout.spec.ts
  - e2e/history.spec.ts
  - tests/stores/useWorkoutStore.test.ts
autonomous: false
requirements:
  - WORK-01
  - WORK-02
  - WORK-03
  - WORK-04
  - WORK-05
  - WORK-06

must_haves:
  truths:
    - "E2E test confirms full workout logging flow works end to end"
    - "E2E test confirms history page shows logged workout"
    - "Store tests confirm session lifecycle (start, add, confirm, finish)"
    - "Offline persistence confirmed via page reload test"
    - "Human verifies visual quality of logger, timer, history, and plan runner"
  artifacts:
    - path: "e2e/workout.spec.ts"
      provides: "End-to-end workout logging and plan runner tests"
      contains: "Start Workout"
    - path: "e2e/history.spec.ts"
      provides: "End-to-end history and persistence tests"
      contains: "History"
    - path: "tests/stores/useWorkoutStore.test.ts"
      provides: "Store unit tests for full session lifecycle"
      contains: "startSession"
  key_links:
    - from: "e2e/workout.spec.ts"
      to: "src/app/(main)/workout/page.tsx"
      via: "page navigation and interaction"
      pattern: "page.goto.*workout"
    - from: "e2e/history.spec.ts"
      to: "src/app/(main)/history/page.tsx"
      via: "page navigation"
      pattern: "page.goto.*history"
---

<objective>
Finalize Phase 4 with comprehensive E2E tests, store unit tests, and human verification of the complete workout logging experience.

Purpose: Validates all 6 requirements (WORK-01 through WORK-06) are met before phase completion.
Output: Green test suite and human sign-off on visual/UX quality.
</objective>

<execution_context>
@/home/appuser/workspace/rip-zone/.claude/get-shit-done/workflows/execute-plan.md
@/home/appuser/workspace/rip-zone/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-workout-logger/04-CONTEXT.md
@.planning/phases/04-workout-logger/04-RESEARCH.md
@.planning/phases/04-workout-logger/04-03-SUMMARY.md
@.planning/phases/04-workout-logger/04-04-SUMMARY.md
@.planning/phases/04-workout-logger/04-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Store unit tests and E2E tests for workout logging, history, and plans</name>
  <files>tests/stores/useWorkoutStore.test.ts, e2e/workout.spec.ts, e2e/history.spec.ts</files>
  <read_first>src/stores/useWorkoutStore.ts, src/app/(main)/workout/page.tsx, src/app/(main)/history/page.tsx, src/lib/db/workouts.ts, src/lib/pr-detection.ts, e2e/history.spec.ts, e2e/exercise-library.spec.ts, tests/stores/useWorkoutStore.test.ts, vitest.config.ts, playwright.config.ts</read_first>
  <action>
1. **Store unit tests** (`tests/stores/useWorkoutStore.test.ts`):
   - Replace .todo() stubs from Plan 01 with real tests
   - Use fake-indexeddb (already in vitest setup) for Dexie operations
   - Tests:
     - `startSession creates a session in Dexie with startedAt and completedAt=null`
     - `addExercise adds ExerciseInSession record to Dexie and appends to activeExercises`
     - `confirmSet creates SetLogRecord in Dexie and updates pendingSet to match confirmed values`
     - `finishSession sets completedAt on session and clears activeSession`
     - `startTimer sets timer state with running=true and correct remaining/total`
     - `pauseTimer sets running=false, resumeTimer sets running=true`
     - `adjustTimer adds delta to remaining, clamps to 0 minimum`
     - `dismissTimer resets timer to initial state`
     - `finishSession with planId saves PlanProgressRecord to planProgress table`
   - Each test: call the action, then verify both Zustand state AND Dexie table contents

2. **E2E workout test** (`e2e/workout.spec.ts`):
   - Tests using Playwright (port 3001 per Phase 1 decision):
     - `freestyle logging flow`: navigate to /workout, click "Start Workout" (or "Start Freestyle Workout"), click "+Exercise", search for "bench" in the picker, select "Barbell Bench Press", verify exercise appears in logger, confirm a set (interact with reps/weight fields and confirm button), click "Finish Workout", verify session ended
     - `plan workout flow` (test.skip if too complex — stub with comment): navigate to /workout, switch to Plans tab, select a plan, tap a day, verify exercises pre-loaded
     - `rest timer appears after set confirm`: verify floating timer widget is visible after confirming a set (if exercise has restSeconds)
   - Use realistic selectors: `page.getByRole('button', { name: /start/i })`, `page.getByPlaceholder(/search/i)`

3. **E2E history test** (`e2e/history.spec.ts`):
   - Replace stubs from Plan 04 with real tests:
     - `empty state`: navigate to /history, verify "No workouts" message visible
     - `shows logged workout` (may need to log a workout first in the test, or use a test helper to seed Dexie): navigate to /workout, complete a quick workout, navigate to /history, verify session row appears
     - `data persists across reload`: after logging workout, reload page, verify history still shows the session (WORK-06)
   - For tests that require seeding data: use `page.evaluate()` to directly insert records into IndexedDB via Dexie
  </action>
  <verify>
    <automated>cd /home/appuser/workspace/rip-zone && npx vitest run tests/stores/useWorkoutStore.test.ts --reporter=verbose 2>&1 | tail -30 && npx playwright test e2e/workout.spec.ts e2e/history.spec.ts --reporter=list 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - tests/stores/useWorkoutStore.test.ts contains `startSession` test (not .todo)
    - tests/stores/useWorkoutStore.test.ts contains `confirmSet` test (not .todo)
    - tests/stores/useWorkoutStore.test.ts contains `finishSession` test (not .todo)
    - e2e/workout.spec.ts contains `page.goto('/workout')`
    - e2e/workout.spec.ts contains `Start` (button interaction)
    - e2e/history.spec.ts contains `No workouts` or `no workouts` (empty state check)
    - e2e/history.spec.ts contains `page.reload()` (persistence test for WORK-06)
    - All vitest store tests pass
    - Playwright tests pass (or skipped tests documented with reason)
  </acceptance_criteria>
  <done>Store tests verify full session lifecycle; E2E tests verify logging flow, history display, and offline persistence</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of workout logger, timer, history, and plan runner</name>
  <what-built>Complete Phase 4 workout logger: freestyle logging with exercise picker, rest timer, PR detection, workout history with contribution graph, and plan runner with recommendations</what-built>
  <how-to-verify>
    1. Navigate to http://localhost:3001/workout
    2. Click "Start Freestyle Workout" — verify workout timer starts at top
    3. Click "+Exercise" — verify bottom sheet opens with search bar and muscle-group tabs
    4. Search for "bench" — verify results filter. Select "Barbell Bench Press"
    5. Verify exercise appears in logger with a set row showing pre-filled reps and weight inputs
    6. Enter weight (e.g., 80kg), tap confirm checkmark — verify set is logged and rest timer appears as floating bubble
    7. Tap the timer bubble — verify expanded controls (pause, +15s, -15s, dismiss)
    8. Log a few more sets. If one beats a theoretical PR, verify neon PR badge appears
    9. Click "Finish Workout" — verify session ends
    10. Navigate to http://localhost:3001/history
    11. Verify the just-completed workout appears in the timeline with date, exercise count, sets, volume
    12. Verify contribution graph shows a colored cell for today
    13. Tap the session row — verify it expands to show per-exercise breakdown with sets
    14. Reload the page — verify all data persists (offline-first confirmation)
    15. Navigate back to /workout, switch to "Plans" tab
    16. Verify plan cards show with difficulty badges and daysPerWeek info
    17. If you have workout history, check for "Recommended for you" badge
    18. Select a plan, tap a day — verify exercises pre-load into the logger
    19. Verify you can add/remove exercises during the plan workout (flexibility per D-15)
    20. Check bottom nav shows 4 tabs: Map, Exercises, History, Workout
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run` — all unit tests pass
- `npx playwright test` — all E2E tests pass
- Human sign-off on visual quality and UX flow
</verification>

<success_criteria>
- WORK-01: Freestyle logging flow works end-to-end (start, pick exercise, enter sets, finish)
- WORK-02: Rest timer counts down with sound/vibration alert
- WORK-03: History shows sessions in reverse chronological order with contribution graph
- WORK-04: PR badges appear for new personal records
- WORK-05: Plan runner pre-loads exercises, tracks progress
- WORK-06: All data persists across page reload (Dexie/IndexedDB)
</success_criteria>

<output>
After completion, create `.planning/phases/04-workout-logger/04-06-SUMMARY.md`
</output>
