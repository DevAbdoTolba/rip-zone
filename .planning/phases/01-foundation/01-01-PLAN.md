---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - components.json
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/lib/utils.ts
  - src/components/ui/button.tsx
  - src/components/ui/badge.tsx
  - src/types/branded.ts
  - src/types/muscle.ts
  - src/types/workout.ts
  - src/types/ranking.ts
  - src/types/strain.ts
  - src/types/bio.ts
  - src/types/index.ts
  - .env.local.example
  - .gitignore
autonomous: true
requirements: []

must_haves:
  truths:
    - "Next.js 16 dev server starts with Turbopack and renders a page"
    - "Dark neon design tokens render correctly in the browser"
    - "All TypeScript domain types compile without errors"
    - "Inter font loads and applies to all text"
  artifacts:
    - path: "src/types/branded.ts"
      provides: "Brand<T,B> helper type for domain IDs"
      exports: ["Brand"]
    - path: "src/types/muscle.ts"
      provides: "MuscleId, MuscleSlug, MuscleGroup, Muscle interface"
      exports: ["MuscleId", "MuscleSlug", "MuscleGroup", "Muscle"]
    - path: "src/types/workout.ts"
      provides: "WorkoutSession, ExerciseLog, SetLog types"
      exports: ["WorkoutSessionId", "ExerciseLogId", "SetLogId", "WorkoutSession", "ExerciseLog", "SetLog"]
    - path: "src/types/ranking.ts"
      provides: "TierRank enum, RankState"
      exports: ["TierRank", "RankState"]
    - path: "src/types/strain.ts"
      provides: "StrainLevel enum"
      exports: ["StrainLevel"]
    - path: "src/types/bio.ts"
      provides: "BioMetricEntry type"
      exports: ["BioMetricEntryId", "BioMetricEntry"]
    - path: "src/app/globals.css"
      provides: "Dark neon OKLCH color tokens"
      contains: "oklch"
    - path: "src/app/layout.tsx"
      provides: "Root layout with Inter font and dark background"
      contains: "Inter"
    - path: "components.json"
      provides: "shadcn configuration"
  key_links:
    - from: "src/types/muscle.ts"
      to: "src/types/branded.ts"
      via: "import Brand"
      pattern: "import.*Brand.*from.*branded"
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "import './globals.css'"
      pattern: "import.*globals.css"
---

<objective>
Bootstrap the Next.js 16 project with full design system and TypeScript domain type contracts.

Purpose: Every subsequent plan and phase depends on the project skeleton existing, the design tokens being locked, and the type contracts being defined. This is the irreversible foundation.
Output: A running Next.js 16 app with dark neon theme, Inter font, shadcn/ui initialized, and all domain types compiled.
</objective>

<execution_context>
@/home/appuser/workspace/rip-zone/.claude/get-shit-done/workflows/execute-plan.md
@/home/appuser/workspace/rip-zone/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap Next.js 16 project and initialize shadcn/ui</name>
  <files>package.json, tsconfig.json, next.config.ts, components.json, src/app/globals.css, src/app/layout.tsx, src/app/page.tsx, src/lib/utils.ts, src/components/ui/button.tsx, src/components/ui/badge.tsx, .env.local.example, .gitignore</files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md (installation sequence, shadcn init, version numbers)
    .planning/phases/01-foundation/01-UI-SPEC.md (color tokens, typography, spacing)
  </read_first>
  <action>
    1. Run `npx create-next-app@latest . --yes` from the project root (the current directory already exists with README.md and .planning/). This bootstraps the Next.js 16 project in-place. If prompted about conflicting files, allow overwrite of README.md. Select:
       - TypeScript: Yes
       - ESLint: Yes
       - Tailwind CSS: Yes
       - src/ directory: Yes
       - App Router: Yes
       - Import alias: @/*

    2. Verify `package.json` has `next` version 16.x, `react` 19.x, `react-dom` 19.x.

    3. Install additional dependencies:
       ```
       npm install mongoose dexie zustand
       npm install -D vitest @testing-library/react @testing-library/dom jsdom tsx
       ```

    4. Run `npx shadcn@latest init` with these settings:
       - Style: Default
       - Base color: Slate (will override with neon)
       - CSS variables: Yes

    5. Install animation support: `npm install tw-animate-css`

    6. Add shadcn components: `npx shadcn@latest add button badge`

    7. Create `.env.local.example` with content:
       ```
       MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/rip-zone?retryWrites=true&w=majority
       ```

    8. Ensure `.gitignore` includes `.env.local` (create-next-app should handle this, verify).

    9. Add to `package.json` scripts:
       ```json
       "seed": "npx tsx scripts/seed.ts",
       "test": "vitest run"
       ```

    10. Override `src/app/globals.css` with the dark neon OKLCH theme. The complete file content:
       ```css
       @import "tailwindcss";
       @import "tw-animate-css";

       @custom-variant dark (&:is(.dark *));

       @theme inline {
         --color-background: var(--background);
         --color-foreground: var(--foreground);
         --color-card: var(--card);
         --color-card-foreground: var(--card-foreground);
         --color-popover: var(--popover);
         --color-popover-foreground: var(--popover-foreground);
         --color-primary: var(--primary);
         --color-primary-foreground: var(--primary-foreground);
         --color-secondary: var(--secondary);
         --color-secondary-foreground: var(--secondary-foreground);
         --color-muted: var(--muted);
         --color-muted-foreground: var(--muted-foreground);
         --color-accent: var(--accent);
         --color-accent-foreground: var(--accent-foreground);
         --color-destructive: var(--destructive);
         --color-border: var(--border);
         --color-input: var(--input);
         --color-ring: var(--ring);
         --color-chart-1: var(--chart-1);
         --color-chart-2: var(--chart-2);
         --color-chart-3: var(--chart-3);
         --color-chart-4: var(--chart-4);
         --color-chart-5: var(--chart-5);
         --color-sidebar: var(--sidebar);
         --color-sidebar-foreground: var(--sidebar-foreground);
         --color-sidebar-primary: var(--sidebar-primary);
         --color-sidebar-primary-foreground: var(--sidebar-primary-foreground);
         --color-sidebar-accent: var(--sidebar-accent);
         --color-sidebar-accent-foreground: var(--sidebar-accent-foreground);
         --color-sidebar-border: var(--sidebar-border);
         --color-sidebar-ring: var(--sidebar-ring);
         --radius-sm: calc(var(--radius) - 4px);
         --radius-md: calc(var(--radius) - 2px);
         --radius-lg: var(--radius);
         --radius-xl: calc(var(--radius) + 4px);
       }

       :root {
         --background: oklch(0.07 0.01 265);
         --foreground: oklch(0.95 0.01 270);
         --card: oklch(0.10 0.02 265);
         --card-foreground: oklch(0.95 0.01 270);
         --popover: oklch(0.10 0.02 265);
         --popover-foreground: oklch(0.95 0.01 270);
         --primary: oklch(0.85 0.18 195);
         --primary-foreground: oklch(0.07 0.01 265);
         --secondary: oklch(0.60 0.25 290);
         --secondary-foreground: oklch(0.95 0.01 270);
         --muted: oklch(0.12 0.02 265);
         --muted-foreground: oklch(0.50 0.02 250);
         --accent: oklch(0.15 0.03 265);
         --accent-foreground: oklch(0.95 0.01 270);
         --destructive: oklch(0.60 0.20 25);
         --border: oklch(0.15 0.03 265);
         --input: oklch(0.15 0.03 265);
         --ring: oklch(0.85 0.18 195);
         --chart-1: oklch(0.85 0.18 195);
         --chart-2: oklch(0.60 0.25 290);
         --chart-3: oklch(0.70 0.20 150);
         --chart-4: oklch(0.70 0.15 60);
         --chart-5: oklch(0.65 0.20 330);
         --radius: 0.5rem;
         --sidebar: oklch(0.10 0.02 265);
         --sidebar-foreground: oklch(0.95 0.01 270);
         --sidebar-primary: oklch(0.85 0.18 195);
         --sidebar-primary-foreground: oklch(0.07 0.01 265);
         --sidebar-accent: oklch(0.15 0.03 265);
         --sidebar-accent-foreground: oklch(0.95 0.01 270);
         --sidebar-border: oklch(0.15 0.03 265);
         --sidebar-ring: oklch(0.85 0.18 195);
       }
       ```

       Note: Dark mode only per D-03. No `.dark` variant needed. All values on `:root`.

    11. Override `src/app/layout.tsx` with:
       ```tsx
       import type { Metadata } from 'next'
       import { Inter } from 'next/font/google'
       import './globals.css'

       const inter = Inter({
         subsets: ['latin'],
         weight: ['400', '600'],
         variable: '--font-inter',
       })

       export const metadata: Metadata = {
         title: 'Rip Zone',
         description: 'Track your workouts and visualize muscle strain',
       }

       export default function RootLayout({ children }: { children: React.ReactNode }) {
         return (
           <html lang="en" className={inter.variable}>
             <body className="bg-background text-foreground font-sans antialiased">
               {children}
             </body>
           </html>
         )
       }
       ```

    12. Replace `src/app/page.tsx` with a minimal placeholder (the real smoke-test page is in Plan 04):
       ```tsx
       export default function Home() {
         return (
           <main className="flex min-h-screen items-center justify-center">
             <h1 className="text-2xl font-semibold text-primary">Rip Zone</h1>
           </main>
         )
       }
       ```

    13. `next.config.ts` should be minimal per RESEARCH.md:
       ```typescript
       import type { NextConfig } from 'next'

       const nextConfig: NextConfig = {
         // Turbopack is the default in Next.js 16 — no opt-in needed
         // mongoose is auto-exempt from serverExternalPackages — no manual config needed
       }

       export default nextConfig
       ```

    14. Verify the dev server starts: `npm run dev` (run briefly, confirm no errors, then stop).
  </action>
  <verify>
    <automated>cd /home/appuser/workspace/rip-zone && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "next" with version starting with "16"
    - package.json contains "mongoose", "dexie", "zustand" in dependencies
    - package.json contains "vitest", "tsx" in devDependencies
    - package.json scripts contains "seed" and "test"
    - components.json exists in project root
    - src/app/globals.css contains "oklch(0.85 0.18 195)" (primary cyan)
    - src/app/globals.css contains "oklch(0.07 0.01 265)" (background)
    - src/app/globals.css does NOT contain ".dark" class selector (dark mode only, no toggle)
    - src/app/layout.tsx contains "Inter" and "next/font/google"
    - src/app/layout.tsx contains "--font-inter"
    - src/app/layout.tsx contains "Rip Zone" in metadata title
    - src/components/ui/button.tsx exists (shadcn button component)
    - src/components/ui/badge.tsx exists (shadcn badge component)
    - .env.local.example contains "MONGODB_URI="
    - next.config.ts exists
    - npx tsc --noEmit exits with code 0
  </acceptance_criteria>
  <done>Next.js 16 project bootstrapped with shadcn/ui, dark neon OKLCH theme, Inter font, and all dependencies installed. Dev server can start without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create all TypeScript domain type contracts</name>
  <files>src/types/branded.ts, src/types/muscle.ts, src/types/workout.ts, src/types/ranking.ts, src/types/strain.ts, src/types/bio.ts, src/types/index.ts</files>
  <read_first>
    .planning/phases/01-foundation/01-CONTEXT.md (D-15 branded types, D-16 no Zod, D-17 enums)
    .planning/phases/01-foundation/01-RESEARCH.md (Pattern 4: branded types, Pattern 3: Dexie interfaces)
  </read_first>
  <action>
    Create all TypeScript domain types per D-15 (branded types), D-16 (no Zod), D-17 (enums).

    **src/types/branded.ts:**
    ```typescript
    declare const __brand: unique symbol
    export type Brand<T, B extends string> = T & { [__brand]: B }
    ```

    **src/types/muscle.ts:**
    ```typescript
    import type { Brand } from './branded'

    export type MuscleId = Brand<string, 'MuscleId'>
    export type MuscleSlug = Brand<string, 'MuscleSlug'>

    export type MuscleGroup =
      | 'chest'
      | 'back'
      | 'shoulders'
      | 'arms'
      | 'forearms'
      | 'core'
      | 'legs'
      | 'glutes'
      | 'calves'

    export interface Muscle {
      id: MuscleId
      slug: MuscleSlug
      displayName: string
      group: MuscleGroup
      svgRegion: string
    }
    ```

    **src/types/workout.ts:**
    ```typescript
    import type { Brand } from './branded'
    import type { MuscleSlug } from './muscle'

    export type WorkoutSessionId = Brand<string, 'WorkoutSessionId'>
    export type ExerciseLogId = Brand<string, 'ExerciseLogId'>
    export type SetLogId = Brand<string, 'SetLogId'>
    export type ExerciseSlug = Brand<string, 'ExerciseSlug'>
    export type WorkoutPlanId = Brand<string, 'WorkoutPlanId'>

    export type EquipmentType =
      | 'barbell'
      | 'dumbbell'
      | 'cable'
      | 'machine'
      | 'bodyweight'
      | 'kettlebell'
      | 'band'
      | 'other'

    export type Difficulty = 'beginner' | 'intermediate' | 'advanced'

    export interface Exercise {
      slug: ExerciseSlug
      name: string
      primaryMuscles: MuscleSlug[]
      secondaryMuscles: MuscleSlug[]
      equipment: EquipmentType
      difficulty: Difficulty
      description: string
      formCues: string[]
    }

    export interface WorkoutSession {
      id: WorkoutSessionId
      startedAt: number
      completedAt: number | null
      planId: WorkoutPlanId | null
    }

    export interface ExerciseLog {
      id: ExerciseLogId
      sessionId: WorkoutSessionId
      exerciseSlug: ExerciseSlug
      orderIndex: number
    }

    export interface SetLog {
      id: SetLogId
      exerciseLogId: ExerciseLogId
      setNumber: number
      reps: number
      weightKg: number
      completedAt: number
    }

    export interface WorkoutPlan {
      id: WorkoutPlanId
      name: string
      slug: string
      goal: string
      difficulty: Difficulty
      daysPerWeek: number
      exercises: WorkoutPlanExercise[]
    }

    export interface WorkoutPlanExercise {
      exerciseSlug: ExerciseSlug
      sets: number
      repsMin: number
      repsMax: number
      restSeconds: number
      orderIndex: number
      dayLabel: string
    }
    ```

    **src/types/ranking.ts:**
    ```typescript
    export enum TierRank {
      Iron = 'Iron',
      Bronze = 'Bronze',
      Silver = 'Silver',
      Gold = 'Gold',
      Platinum = 'Platinum',
      Diamond = 'Diamond',
      Elite = 'Elite',
    }

    export interface RankState {
      id: 'singleton'
      tier: TierRank
      subTierProgress: number
      lastUpdatedAt: number
    }
    ```

    **src/types/strain.ts:**
    ```typescript
    export enum StrainLevel {
      Rested = 'Rested',
      Light = 'Light',
      Moderate = 'Moderate',
      Heavy = 'Heavy',
      Strained = 'Strained',
    }
    ```

    **src/types/bio.ts:**
    ```typescript
    import type { Brand } from './branded'

    export type BioMetricEntryId = Brand<string, 'BioMetricEntryId'>

    export interface BioMetricEntry {
      id: BioMetricEntryId
      recordedAt: number
      heightCm: number | null
      weightKg: number | null
      ageYears: number | null
      gender: 'male' | 'female' | null
      bodyFatPct: number | null
    }
    ```

    **src/types/index.ts:**
    ```typescript
    export type { Brand } from './branded'
    export type { MuscleId, MuscleSlug, MuscleGroup, Muscle } from './muscle'
    export type {
      WorkoutSessionId,
      ExerciseLogId,
      SetLogId,
      ExerciseSlug,
      WorkoutPlanId,
      EquipmentType,
      Difficulty,
      Exercise,
      WorkoutSession,
      ExerciseLog,
      SetLog,
      WorkoutPlan,
      WorkoutPlanExercise,
    } from './workout'
    export { TierRank } from './ranking'
    export type { RankState } from './ranking'
    export { StrainLevel } from './strain'
    export type { BioMetricEntryId, BioMetricEntry } from './bio'
    ```

    Note: The index.ts barrel export uses `export type` for interfaces and branded types, and value `export` for enums (TierRank, StrainLevel) since they have runtime representation.
  </action>
  <verify>
    <automated>cd /home/appuser/workspace/rip-zone && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/types/branded.ts contains "declare const __brand: unique symbol"
    - src/types/branded.ts contains "export type Brand<T, B extends string>"
    - src/types/muscle.ts contains "export type MuscleId = Brand<string, 'MuscleId'>"
    - src/types/muscle.ts contains "export type MuscleSlug = Brand<string, 'MuscleSlug'>"
    - src/types/muscle.ts contains "export type MuscleGroup ="
    - src/types/muscle.ts contains "export interface Muscle"
    - src/types/workout.ts contains "export type WorkoutSessionId ="
    - src/types/workout.ts contains "export type ExerciseSlug ="
    - src/types/workout.ts contains "export interface WorkoutSession"
    - src/types/workout.ts contains "export interface ExerciseLog"
    - src/types/workout.ts contains "export interface SetLog"
    - src/types/workout.ts contains "export interface Exercise"
    - src/types/workout.ts contains "export interface WorkoutPlan"
    - src/types/ranking.ts contains "export enum TierRank"
    - src/types/ranking.ts contains "Iron = 'Iron'"
    - src/types/ranking.ts contains "Elite = 'Elite'"
    - src/types/ranking.ts contains "export interface RankState"
    - src/types/strain.ts contains "export enum StrainLevel"
    - src/types/strain.ts contains "Rested = 'Rested'"
    - src/types/strain.ts contains "Strained = 'Strained'"
    - src/types/bio.ts contains "export type BioMetricEntryId ="
    - src/types/bio.ts contains "export interface BioMetricEntry"
    - src/types/index.ts contains "export type { MuscleId" (barrel re-export)
    - src/types/index.ts contains "export { TierRank }" (value export for enum)
    - src/types/index.ts contains "export { StrainLevel }" (value export for enum)
    - npx tsc --noEmit exits with code 0
  </acceptance_criteria>
  <done>All TypeScript domain types compile: MuscleId, MuscleSlug, MuscleGroup, Muscle, WorkoutSession, ExerciseLog, SetLog, Exercise, WorkoutPlan, TierRank, RankState, StrainLevel, BioMetricEntry, and all branded ID types. Barrel export at src/types/index.ts provides single import point.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` exits with code 0 — all types compile
2. `components.json` exists — shadcn initialized
3. `src/app/globals.css` contains OKLCH variables — dark neon theme configured
4. `src/app/layout.tsx` loads Inter font — typography configured
5. Dev server starts without errors (manual check during Task 1)
</verification>

<success_criteria>
- Next.js 16 project exists with src/ directory structure
- All npm dependencies installed (next, react, mongoose, dexie, zustand, shadcn, vitest, tsx)
- shadcn/ui initialized with dark base theme and OKLCH neon overrides
- Inter font loaded via next/font/google
- All 7 domain type files compile without errors
- Branded types enforce compile-time safety for domain IDs
- Barrel export at src/types/index.ts re-exports all types
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
